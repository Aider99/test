<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Smart_learner">
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Mathematic"
            android:label="@string/button_menu1">
        </activity>
        <activity android:name=".Rus_antonyms"
            android:label="@string/button_menu2">
        </activity>
        <activity android:name=".Rus_synonyms"
            android:label="@string/button_menu2">
        </activity>

        <activity android:name=".Music"
            android:label="@string/button_menu5">
        </activity>

        <activity android:name=".Puzzle"
            android:label="@string/button_menu3">
        </activity>

        <activity android:name=".Fifteen"
            android:label="@string/button_menu6">
        </activity>

        <activity android:name=".ShapeSelect"> </activity>
        <activity android:name=".ColorChooseView"> </activity>
        <activity android:name=".Paint"> </activity>
    </application>

</manifest>